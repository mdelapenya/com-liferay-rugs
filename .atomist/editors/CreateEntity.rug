@description "Creates a java file for an entity, passed as parameter"
editor CreateEntity

param entity_name: ^.*$

let className = { "src/main/java/" + entity_name + ".java" }
let classContent = { "class " + entity_name + " {}" }

let template = "Entity.vm"

with Project
	do merge template to className