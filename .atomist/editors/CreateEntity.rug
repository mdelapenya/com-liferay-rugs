@description "Creates a java file for an entity, passed as parameter"
editor CreateEntity

param entityName: ^.*$

let className = { "src/main/java/" + entityName + ".java" }
let classContent = { "class " + entityName + " {}" }

let template = "Entity.vm"

with Project
	do merge template to className